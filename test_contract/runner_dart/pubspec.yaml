name: personal_os_contract_runner
description: Honest, deterministic Dart runner for Personal OS contract fixtures.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  personal_os_domain:
    path: ../../packages/domain
  personal_os_events:
    path: ../../packages/events
  personal_os_in_memory:
    path: ../../adapters/in_memory
  personal_os_policy:
    path: ../../packages/policy

dev_dependencies:
  test: ^1.25.0
