# sync_api

Pure Dart synchronization port over immutable event batches and opaque cursors.
Relay, HTTP, WebSocket, CRDT, and database-specific types are intentionally
absent. Device trust and payload encryption are adapter/policy concerns.

