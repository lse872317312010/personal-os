name: personal_os_sync_api
description: Transport-neutral synchronization ports for Personal OS.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  personal_os_events:
    path: ../events

