name: personal_os_blob_engine
description: Trusted local encrypted blob orchestration for Personal OS.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  personal_os_domain:
    path: ../domain
  personal_os_security_api:
    path: ../security_api
  personal_os_source_api:
    path: ../source_api
  personal_os_storage_api:
    path: ../storage_api

dev_dependencies:
  test: ^1.25.0
