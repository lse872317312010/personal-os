name: personal_os_storage_api
description: Vendor-neutral persistence ports for Personal OS.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  personal_os_domain:
    path: ../domain
  personal_os_events:
    path: ../events

dev_dependencies:
  test: ^1.25.0
