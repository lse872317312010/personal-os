name: personal_os_source_api
description: Adapter-neutral controlled local media source contract for Personal OS.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
