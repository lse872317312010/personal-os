name: personal_os_runtime
description: Platform-neutral fail-closed runtime lifecycle coordinator.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
