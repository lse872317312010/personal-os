name: personal_os_in_memory_policy
description: In-memory policy composition adapters for Personal OS tests and spikes.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  personal_os_domain:
    path: ../../packages/domain
  personal_os_policy:
    path: ../../packages/policy
  personal_os_policy_application:
    path: ../policy_application

dev_dependencies:
  test: ^1.25.0
