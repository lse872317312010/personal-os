name: personal_os_policy_application
description: Production policy adapter for Personal OS application use cases.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  personal_os_application:
    path: ../../packages/application
  personal_os_domain:
    path: ../../packages/domain
  personal_os_policy:
    path: ../../packages/policy
  personal_os_events:
    path: ../../packages/events
  personal_os_storage_api:
    path: ../../packages/storage_api

dev_dependencies:
  test: ^1.25.0
